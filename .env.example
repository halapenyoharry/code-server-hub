# Code Server Hub Configuration

# Server port
PORT=7777

# Environment
NODE_ENV=production

# Data directory for code-server instances
# Default: ~/code-server-data
CODE_SERVER_DATA_DIR=~/code-server-data

# Path to code-server executable
# Default: code-server (assumes it's in PATH)
CODE_SERVER_BIN=code-server